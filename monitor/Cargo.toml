[package]
name = "veridian-monitor"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "veridian-monitor"
path = "src/main.rs"

[dependencies]
spin = { version = "0.9", default-features = false, features = ["spin_mutex"] }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
